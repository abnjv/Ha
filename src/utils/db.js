import { get, set, del } from 'idb-keyval';

/**
 * A simple key-value store using IndexedDB.
 * This is used to persist cryptographic keys and other sensitive data
 * on the client-side.
 */

export { get, set, del };
